# Runtime Threat Detection 基礎 — ウェブエンジニア向け実践ガイド

> **対象:** ウェブエンジニア（SQLインジェクション等の基礎セキュリティ知識あり）が **KCSA** 試験対策として学ぶ "実行時脅威検知" のエッセンシャル。
> **範囲:** Falco・eBPF / Syscall 監視 / Falco ルール / `/etc/shadow` 読取検知デモ
> **目標:** Webアプリケーションの監視・ログ知識を活かし、k8s 固有の実行時脅威検知を理解

---

## 1. 概念図：Syscall 監視フロー（攻撃者視点）

```
┌──────────────┐  syscalls   ┌────────────────┐  rules   ┌────────────────┐
│   Linux VM   │───────────▶│   eBPF probe   │─────────▶│     Falco       │
└──────────────┘            └────────────────┘          └────────────────┘
        ▲                            │                       │ alert
        │ container runtime          ▼                       ▼
┌──────────────┐           ┌────────────────┐        ┌──────────────────┐
│  Pod / Proc  │──────────▶│ Falco Ruleset │────────▶│  Log / Webhook   │
└──────────────┘           └────────────────┘        └──────────────────┘
```

**攻撃ベクトル:**
- **Syscall 監視**: 特権システムコール・ファイルアクセス・プロセス作成
- **Falco ルール**: 異常な動作パターン・攻撃シグネチャ・権限昇格
- **アラート**: ログ改ざん・監視回避・通知漏洩

---

## 2. ウェブエンジニア向けベストプラクティス

| 領域             | ベストプラクティス                                                 | ウェブアプリとの関連性                                    |
| -------------- | --------------------------------------------------------- | ----------------------------------------------- |
| **Falco デプロイ** | *Helm で DaemonSet として全ノードに導入し、Syslog & Webhook で SIEM 連携* | Webアプリのログ監視・アラート・SIEM連携に類似              |
| **ルール管理**      | *公式ルールをベースに `falco-rules.local.yaml` でカスタム追加、GitOps 管理*   | Webアプリのログ解析・パターンマッチング・異常検知に類似        |
| **アラート運用**     | *Severity 以上のイベントを Slack / PagerDuty に自動転送し、SLO を設定*      | Webアプリの監視ダッシュボード・アラート・インシデント対応に類似  |

---

## 3. Hands‑on ラボ（30 分）

### 3.1 Falco インストール (kind + Helm)

```bash
# 0) Helm repo 追加
helm repo add falcosecurity https://falcosecurity.github.io/charts
helm repo update

# 1) DaemonSet として導入 (Falco 0.38 例)
helm install falco falcosecurity/falco \
  --namespace falco --create-namespace \
  --set falco.jsonOutput=true \
  --set falco.logLevel=info

# 2) Pod 起動確認
kubectl -n falco get pods -l app.kubernetes.io/name=falco
```

### 3.2 `/etc/shadow` 読取を検出するテスト

```bash
# 1) テスト Pod 起動し /etc/shadow を読む
kubectl run read-shadow --image=busybox --restart=Never -- \
  sh -c "cat /etc/shadow || true"

# 2) Falco ログ確認 (数秒待機)
kubectl -n falco logs -l app.kubernetes.io/name=falco --tail=50 | \
  grep "shadow" | head
```

> **期待結果:** `Sensitive file opened for reading` イベントとして `/etc/shadow` パスが含まれた Falco アラートが出力される。

### 3.3 カスタムルールを追加 (任意)

```bash
cat <<'EOF' > custom-rules.yaml
- rule: Detect vi usage in container
  desc: Someone used vi inside a container
  condition: container and proc.name = vi
  output: "Container exec detected (vi) user=%user.name container=%container.name (%container.id)"
  priority: Warning
  tags: [container, shell]
EOF

helm upgrade falco falcosecurity/falco -n falco -f custom-rules.yaml --set falco.rulesFile.append=true
```

---

## 4. チェックリスト ✅ / ❌

* [ ] Falco Pod が eBPF ドライバモードで起動していることを確認 (`falco-driver-loader`)
* [ ] `/etc/shadow` 読取が Falco でアラートになることを確認
* [ ] `falco-rules.local.yaml` で自作ルールを 1 つ作成しトリガーした
* [ ] Webhook で Slack 送信テストが成功
* [ ] ウェブアプリケーションの **監視・ログ** との関連性を理解している

---

## 5. 推奨リソース

* Falco Official Docs — Getting Started / Rules
* eBPF & Falco — CNCF Webinar "Runtime Security with eBPF"
* Blog: "Detecting Kubernetes Threats with Falco"
* GitHub: `falcosecurity/falco-examples`
* OWASP Logging Cheat Sheet
* SIEM Integration Best Practices

---

### エンドノート

このガイドで **eBPF Syscall 監視 → ルール検知 → アラート出力** の一連を体験できます。Falco を GitOps で管理し、CI でルール lint を走らせると運用が安定します。ウェブエンジニアの既存監視・ログ知識を活かし、k8s 固有の実行時脅威検知を理解してください。
