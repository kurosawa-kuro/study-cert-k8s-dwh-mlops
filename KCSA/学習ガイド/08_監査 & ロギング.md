# Audit & Logging 基礎 — ウェブエンジニア向け実践ガイド

> **対象:** ウェブエンジニア（SQLインジェクション等の基礎セキュリティ知識あり）が KCSA 試験対策として習得すべき "監査 & ロギング" の必須トピック。
> **範囲:** Audit Policy 設定 / Fluent Bit 集約 / `kubectl exec` イベント検証
> **キーワード:** `audit-policy.yaml`・AuditSink・Fluent Bit・Loki / Elasticsearch
> **目標:** Webアプリケーションのログ・監査知識を活かし、k8s 固有の監査機能を理解

---

## 1. A4 図解：監査ログ流れ & 集約パイプライン（攻撃者視点）

```
┌──────────────┐   req/res   ┌────────────────┐  audit event  ┌──────────────────┐
│  kubectl     │───────────▶│ kube‑apiserver │──────────────▶│ audit log file   │
└──────────────┘            └────────────────┘               └──────────────────┘
                                                                    │ tail
                                                                    ▼
                                                            ┌────────────────┐
                                                            │  Fluent Bit    │
                                                            │  (DaemonSet)   │
                                                            └────────────────┘
                                                                    │ forward
                                                                    ▼
                                                      ┌──────────────┐  ┌─────────────┐
                                                      │   Loki       │  │ Elasticsearch │
                                                      └──────────────┘  └─────────────┘
```

**攻撃ベクトル:**
- **Audit Policy**: ログ改ざん・監査回避・証跡削除
- **Log Rotation**: ディスク枯渇・ログ上書き・バックアップ漏洩
- **Centralized Logs**: データ漏洩・不正アクセス・検索回避

---

## 2. ウェブエンジニア向けベストプラクティス

| 領域                   | ベストプラクティス                                                                          | ウェブアプリとの関連性                                    |
| -------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------- |
| **Audit Policy**     | *`Stage=RequestResponse` を高感度 API に限定し、残りは `Metadata` でノイズ削減*                      | Webアプリのアクセスログ・セキュリティログ・監査ログに類似        |
| **Log Rotation**     | *kube‑apiserver の `--audit-log-maxsize=100` `--audit-log-maxbackup=10` でディスク枯渇を防止* | Webアプリのログローテーション・アーカイブ・ストレージ管理に類似    |
| **Centralized Logs** | *Fluent Bit → Loki / Elasticsearch へ JSON 出力し、`verb=exec` などでクエリしやすくする*            | Webアプリのログ集約・検索・分析・アラートに類似              |

---

## 3. Hands‑on ラボ（35 分）

### 3.1 kind クラスタで Audit Log 有効化

> **ファイル構成** (ローカル)
>
> ```
> audit/
> ├─ audit-policy.yaml
> └─ kind-config.yaml
> ```

#### (1) `audit-policy.yaml` — `kubectl exec` を RequestResponse で収集

```yaml
apiVersion: audit.k8s.io/v1
kind: Policy
rules:
  # 1. exec/attach/portforward は詳細捕捉
  - level: RequestResponse
    verbs: ["exec", "attach", "portforward"]
    resources:
      - group: ""
        resources: ["pods/exec", "pods/attach", "pods/portforward"]
  # 2. それ以外はメタデータ
  - level: Metadata
```

#### (2) `kind-config.yaml` — API Server へマウント & 起動引数

```yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
kubeadmConfigPatches:
- |
  kind: ClusterConfiguration
  apiServer:
    extraArgs:
      audit-policy-file: /etc/kubernetes/audit/audit-policy.yaml
      audit-log-path: /var/log/kubernetes/audit.log
      audit-log-maxsize: "100"
      audit-log-maxbackup: "5"
    extraVolumes:
    - name: audit
      hostPath: /etc/kubernetes/audit
      mountPath: /etc/kubernetes/audit
      readOnly: true
nodes:
- role: control-plane
  extraMounts:
  - hostPath: ./audit
    containerPath: /etc/kubernetes/audit
```

```bash
kind create cluster --config audit/kind-config.yaml
```

### 3.2 `kubectl exec` を発生させログ確認

```bash
# 1) テスト Pod
kubectl run alpine --image=alpine --command -- sleep 600

# 2) exec 実行
kubectl exec alpine -- cat /etc/os-release

# 3) Log 確認 (コンテナ内)
CONTROL_PLANE=$(docker ps --filter label=io.x-k8s.kind.role=control-plane -q)
docker exec $CONTROL_PLANE grep "verb=\"exec\"" /var/log/kubernetes/audit.log | head -1 | jq '.'
```

> **期待:** `requestURI":"/api/v1/namespaces/default/pods/alpine/exec` が含まれる JSON エントリが出力。

### 3.3 Fluent Bit で中央集約 (Loki 例)

```bash
# 1) Loki & Grafana 共通チャート
helm repo add grafana https://grafana.github.io/helm-charts && helm repo update
helm install loki grafana/loki-stack --namespace logging --create-namespace \
  --set promtail.enabled=false

# 2) Fluent Bit DaemonSet (抜粋)
cat <<'EOF' | kubectl apply -f -
apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  namespace: logging
# ... (Parser audit JSON → Loki labels)
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluent-bit
  namespace: logging
spec:
  template:
    spec:
      containers:
      - name: fluent-bit
        image: cr.fluentbit.io/fluent/fluent-bit:2.2
        volumeMounts:
        - name: auditlog
          mountPath: /var/log/kubernetes
      volumes:
      - name: auditlog
        hostPath:
          path: /var/log/kubernetes
EOF
```

---

## 4. チェックリスト ✅ / ❌

* [ ] `audit-policy.yaml` で exec/attach だけ `RequestResponse` になっている
* [ ] `kubectl exec` イベントが audit.log に JSON で記録された
* [ ] Fluent Bit が audit.log を Loki (or ES) に転送し、`{verb="exec"}` でクエリ成功
* [ ] ログローテーション設定が効いてファイルサイズが超過してもバックアップされる
* [ ] ウェブアプリケーションの **ログ・監査** との関連性を理解している

---

## 5. 推奨リソース

* Kubernetes Docs — **Auditing** / **Audit Policy**
* CNCF Blog: "Centralized Logging with Fluent Bit & Loki"
* Fluent Bit Official Guides — Kubernetes Tail Input
* Loki LogQL Cheat Sheet
* OWASP Logging Cheat Sheet
* SIEM Integration Best Practices

---

### エンドノート

このガイドでは **Audit Policy → ログ生成 → Fluent Bit 集約 → クエリ検証** まで一気通貫で体験できます。本番では API Server Static Pod のマニフェストに audit オプションを追記し、Fluent Bit を DaemonSet ＋ Secret で S3 等に転送する構成が一般的です。ウェブエンジニアの既存ログ・監査知識を活かし、k8s 固有の監査機能を理解してください。
