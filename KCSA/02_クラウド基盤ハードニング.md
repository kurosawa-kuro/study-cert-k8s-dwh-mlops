# クラウド基盤ハードニング — IAM・ネットワーク分離・Audit

> **対象:** CKAD 85 %・Killer.sh 70 % の Kubernetes エンジニアが KCSA 対策で押さえるべき **AWS 基盤セキュリティ** の要点
> **範囲:** IAM 最小権限 / VPC PrivateLink / CloudTrail + IAM Policy Simulator による過剰権限検出

---

## 1. A4 図解：3 本柱と相互関係

```
┌──────────────────────────────────────────────┐
│                IAM (Identity)               │
│  ── Principle of Least Privilege            │
│  ── IAM Roles / Policies / SCP              │
└──────────────────────────────────────────────┘
            ↓  attaches permissions      ↑ simulate
┌──────────────────────────────────────────────┐
│         Network Segmentation (VPC)           │
│  ── Private Subnet / Security Group          │
│  ── VPC Endpoint & PrivateLink               │
└──────────────────────────────────────────────┘
            ↓  logs API calls                 ↑ informs
┌──────────────────────────────────────────────┐
│               Audit & Monitoring             │
│  ── CloudTrail (Org‑Trail) + S3 Log Bucket   │
│  ── EventBridge + Security Hub / GuardDuty   │
└──────────────────────────────────────────────┘
```

---

## 2. 1 行ベストプラクティス

| カテゴリ       | ベストプラクティス                                                                       |
| ---------- | ------------------------------------------------------------------------------- |
| **IAM**    | *職務ロールごとに **Allow‑list ポリシー** を定義し、**AWS IAM Policy Simulator** で過剰権限を検出してから適用* |
| **ネットワーク** | *パブリック API 通信を **VPC PrivateLink / Interface Endpoint** に置き換え、ルートテーブルを最小限に*     |
| **Audit**  | *全リージョンで **Organization‑trail** を有効化し、ログを S3 + SSE‑KMS で 7 年保管*                 |

---

## 3. Hands‑on ラボ（30 分）

### 3.1 IAM 最小権限ロールの作成 & シミュレーション

```bash
# (1) 最小権限ポリシーを JSON で用意
cat <<'EOF' > ec2ReadOnly.json
{
  "Version": "2012-10-17",
  "Statement": [{
    "Effect": "Allow",
    "Action": ["ec2:Describe*"],
    "Resource": "*"
  }]
}
EOF
aws iam create-policy --policy-name EC2ReadOnly --policy-document file://ec2ReadOnly.json

# (2) ロールへアタッチ
aws iam create-role --role-name DevObserver \
  --assume-role-policy-document file://<(aws iam get-policy-version \
     --policy-arn arn:aws:iam::aws:policy/ServiceRolePolicy --version-id v1 | jq .PolicyVersion.Document)
aws iam attach-role-policy --role-name DevObserver --policy-arn arn:aws:iam::<ACCOUNT_ID>:policy/EC2ReadOnly

# (3) Policy Simulator で検証 (CLI)
aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::<ACCOUNT_ID>:role/DevObserver \
  --action-names ec2:TerminateInstances
# ↑ Should return "Decision": "implicitDeny"
```

### 3.2 VPC PrivateLink で API 通信を内部化

```bash
# パブリック S3 → Interface Endpoint 例
aws ec2 create-vpc-endpoint --vpc-id vpc-xxxx \
  --service-name com.amazonaws.ap-northeast-1.s3 \
  --vpc-endpoint-type Gateway --route-table-ids rtb-xxxx
```

### 3.3 CloudTrail 組織トレイルの有効化

```bash
aws cloudtrail create-trail --name org-trail \
  --is-organization-trail --s3-bucket-name org-trail-logs-<ACCOUNT_ID> \
  --kms-key-id alias/org-trail-key
aws cloudtrail start-logging --name org-trail
```

> **確認:** GuardDuty や Security Hub を有効化し、CloudTrail イベントを統合して異常検知のシグナルを増幅。

---

## 4. チェックリスト ✅ / ❌

* [ ] IAM ポリシー構文 (Version / Statement / Action / Resource) を即記述できる
* [ ] IAM Policy Simulator CLI で **implicitDeny / explicitDeny** を区別できる
* [ ] VPC Endpoints の **Gateway vs Interface** の違いを説明できる
* [ ] CloudTrail が **証跡を残さない操作例** を 1 つ挙げられる (例: S3 GET Object デフォルト)

---

## 5. 推奨リソース

* AWS セキュリティホワイトペーパー “Security Pillar — Well‑Architected”
* AWS IAM Policy Simulator ドキュメント & Labs
* AWS Prescriptive Guidance “Network segmentation patterns”
* CloudTrail Best Practices — AWS Official Blog

---

### エンドノート

この 1 枚で **IAM ➝ ネットワーク ➝ Audit** の連携イメージを掴めます。実プロジェクトでは **CI/CD フェーズでポリシーシミュレータを自動実行** し、PR デフォルトチェックとして過剰権限をブロックする運用が効果的です。さらなる内容追加や質問があればお知らせください！
