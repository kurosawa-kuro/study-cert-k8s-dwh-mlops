# k8s コア概念セキュリティ復習ガイド（KCSA 0. 前提確認）

CKAD で培った基礎を **Security 視点** で再確認し、KCSA 学習をスムーズに進めるためのコンパクト教材です。範囲は次の 4 トピックのみ。

> Pod / Deployment / Service / RBAC ＋ `kubectl auth can-i` 権限チェック

---

## 1. 学習ゴール

* 各リソースの **役割・相互関係** を 5 分以内で説明できる
* 最小構成の **hands‑on コマンド** を暗記し、実機で即実行できる
* `kubectl auth can-i` を使った **権限可視化** と **トラブルシュート** が習慣化している

---

## 2. クイック表（エッセンスまとめ）

| リソース                   | 要点                    | 代表 CLI                                                | セキュリティ観点                                           |
| ---------------------- | --------------------- | ----------------------------------------------------- | -------------------------------------------------- |
| **Pod**                | Linux プロセスの最小単位       | `kubectl run nginx --image nginx`                     | `securityContext`, requests/limits, readOnlyRootFS |
| **Deployment**         | **宣言的ローリング更新** の主役    | `kubectl set image deploy/nginx nginx=nginx:1.27`     | `maxUnavailable`, ImagePullPolicy, サージ制御           |
| **Service**            | Pod の **抽象 EndPoint** | `kubectl expose deploy nginx --port 80`               | タイプ別の公開範囲、NetworkPolicy との併用                       |
| **RBAC**               | API リソース操作権の **ACL**  | `kubectl create role demo --verb=get --resource=pods` | 最小権限原則、Impersonation、Aggregated CR                 |
| **kubectl auth can-i** | 権限テスト                 | `kubectl auth can-i create pods -n dev`               | 失敗原因切り分け、CI での権限テスト                                |

---

## 3. Hands‑on スクリプト（10 分で完走）

> **前提:** kind または minikube でクラスタ起動済み

### 3.1 Pod → Deployment → Service

```bash
# Pod を単発で起動し YAML を観察
kubectl run demo-pod --image=nginx:1.25 --restart=Never
kubectl get pod demo-pod -o yaml | less

# Deployment を作りローリング更新
kubectl create deployment demo --image=nginx:1.25 --replicas=2
kubectl rollout status deployment/demo
kubectl set image deployment/demo nginx=nginx:1.27

# Service で公開して Pod に curl
kubectl expose deployment demo --port 80 --type ClusterIP
CLUSTER_IP=$(kubectl get svc demo -o jsonpath='{.spec.clusterIP}')
kubectl run -i --tty tmp-shell --image=busybox --restart=Never -- sh -c "wget -qO- http://${CLUSTER_IP}"
```

### 3.2 RBAC 最小権限 & 権限チェック

```bash
# 名前空間と ServiceAccount
kubectl create namespace dev
kubectl create sa reader -n dev

# Pod 読み取り専用 Role と Binding
kubectl create role pod-reader --verb=get,list,watch --resource=pods -n dev
kubectl create rolebinding pod-reader-binding --role=pod-reader --serviceaccount=dev:reader -n dev

# 権限確認
kubectl auth can-i get pods --as system:serviceaccount:dev:reader -n dev        # -> yes
kubectl auth can-i delete pods --as system:serviceaccount:dev:reader -n dev     # -> no

# すべての権限を一覧表示
kubectl auth can-i --list --as system:serviceaccount:dev:reader -n dev | less
```

---

## 4. チェックリスト ✅ / ❌

* [ ] Pod Lifecycle を説明できる
* [ ] Deployment のローリングアップデート戦略を 2 つ言える
* [ ] Service タイプ 4 種類を列挙できる
* [ ] Role と ClusterRole の違いを説明できる
* [ ] `kubectl auth can-i` の失敗パターンを 3 つ例示できる

> **活用法:** プルリクやトラブル対応時に **表 & コマンド** を即参照して、Security 視点の抜け漏れを防止！

---

## 5. 推奨リソース

* Kubernetes 公式ドキュメント **Concepts**（Pod, Deployment, Service, RBAC）
* 「Kubernetes RBAC Good Practices」ブログシリーズ
* **kubectl cheat‑sheet** — auth セクション
* Killer.sh CKAD 解説ノート（自分のメモでもOK）

---

### おわりに

このガイドは “覚えているか自信がない項目を即復習できる 1 枚ペーパー” を目指しました。ほかに追加したい項目や不明点があれば、気軽にご連絡ください！
